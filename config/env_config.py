# Environment-specific configuration

ENV_SETTINGS = {
    "window_size": 50,
    "max_drawdown": 0.3,
    "reward_type": "sharpe",    # or "sortino", "custom"
    "commission": 0.001,
    "slippage": 0.0005,
}
